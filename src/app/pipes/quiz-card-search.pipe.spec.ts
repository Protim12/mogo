import { QuizCardSearchPipe } from './quiz-card-search.pipe';

describe('QuizCardSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new QuizCardSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
